{
  "name": "climate-paper-radar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "update": "node scripts/daily.mjs",
    "check": "node --check app.js && node --check server.mjs && node --check scripts/daily.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
